package main

import (
	"fmt"
	"github.com/damiensedgwick/op/utils"
)

func main() {
	// when completed, the goal is to open the link in the browser and print it for reference.
	fmt.Println(utils.ReturnCorrectLink("react"))
}
